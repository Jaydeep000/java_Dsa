package AllSorting;
//import the package
import java.util.Arrays;
public class s4_inbuildSort {
    //arrays.sort();
    //Arrays.sort(arr,starting index,endindex) 0 to 2 then ending should be 3

    //reverse the array
    //import java.util.Collections;
    //Arrays.sort(arr, Collections.reverseOrder())
    //Arrays.sort(arr,starting index,endindex, Collections.reverseOrder())

    //reverse order always works with object...like Integer
}
